/**
 * Listens to every event where a member joins the Guild
 * @async
 * @method
 * @param {GuildMember} member - The member that joined the server
 */
export const guildMemberAddListener = async (member) => {
    // What to do when a member joins the server

}

/**
 * Listens to every event where a member leaves the Guild
 * @async
 * @method
 * @param {GuildMember} member - The member that left the server
 */
export const guildMemberRemoveListener = async (member) => {
    // What to do when a member leaves the server

}