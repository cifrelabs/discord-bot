/**
 * Handles all interactions that are sent from a modal.
 * @async
 * @method
 * @param {Object} interaction - Represents an interaction.
 */
export const modalHandler = async (interaction) => {
    const { customId } = interaction;

    switch (customId) {
        default:
            break;
    }
}